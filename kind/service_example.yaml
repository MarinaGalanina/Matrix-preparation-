apiVersion: v1
metadata:
  name: smth
  labels:
    application: application
    type: env_type
  spec:
    selector:
      application: application
      type: env_type
    type: "ClusterIP"
    ports:
      port: port
      targetPort: port

